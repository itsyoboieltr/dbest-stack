FROM imbios/bun-node:latest-iron-alpine

ARG PORT
ARG DATABASE_URL
ARG VITE_HOST_URL

ENV PORT=$PORT
ENV DATABASE_URL=$DATABASE_URL
ENV VITE_HOST_URL=$VITE_HOST_URL

WORKDIR /app
COPY . .

RUN bun i
RUN bun run build

EXPOSE $PORT

CMD ["bun", "start"]
